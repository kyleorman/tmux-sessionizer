# Add to ~/.config/i3/config
# Launch tmux-sessionizer in a fresh terminal with Mod+\
bindsym $mod+backslash exec --no-startup-id "alacritty -e zsh -lc 'source ~/.zshrc; /usr/local/bin/tmux-sessionizer.sh; exec zsh'"
